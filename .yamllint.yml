---
extends: default

ignore: |
  getrafty/

rules:
  document-start: disable
  line-length:
    max: 200
    level: warning
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1

yaml-files:
  - "*.yaml"
  - "*.yml"
