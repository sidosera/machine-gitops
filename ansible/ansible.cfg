[defaults]
# Inventory: ansible/inventory/localhost.yml; play 1 add_host's group hm from local-env.yaml.
# Target runs k3s; workloads live in namespace hm (see k8s/).
roles_path = roles
interpreter_python = auto_silent
remote_tmp = /tmp/.ansible-${USER}/tmp
