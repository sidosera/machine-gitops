---
- name: Upload secrets bundle (local-env.yaml) from controller to target
  ansible.builtin.copy:
    src: "{{ local_env_yaml_src }}"
    dest: "{{ hm_secrets_dest }}"
    owner: root
    group: root
    mode: "0600"
